实战：不定时的给予一个随机好汉的数据，将给予的水浒108好汉按照顺序记录下来

链表介绍：
链表是有序的列表，但是在内存中是存储如下：
单链表示意图：
        头指针     地址  data域   next域
head    150       110   a2      180
                  120
                  130   a4      170
                  140   a6      NULL
                  150   a1      110
                  160
                  170   a5      140
                  180   a3      130
总结：
    链表是以节点的方式来存储；
    每个节点包含data域，next域：指向下一个节点；
    如图，每个链表的各个节点不一定是连续存储；
    链表分带头节点的链表和没有头节点的链表，根据实际需求来确定。

思路：
1.单链表实现，先简单的将添加的好汉数据加入链表尾部。
2.根据排名将英雄插入到指定位置（如果有这个排名将添加失败并且给出错误提示）